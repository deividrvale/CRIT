THEORY ints     ;
LOGIC QF_LIA    ;
SOLVER internal ;

SIGNATURE add2, return ;

RULES
    add2(x, y) -> add2(x + 1, y - 1) [y > 0] ;
    add2(x, y) -> return(x) [ y = 0 ] ;
    add2(x, y) -> return(0) [ y < 0 ] ;

QUERY equivalence  add2(x,y) -><- return(x + y) [ x >= 0 /\ y >= 0];

END OF FILE

